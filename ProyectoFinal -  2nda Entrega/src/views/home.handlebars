<div>

    {{#each productsList}}
    <div>
        <p>{{this.title}}</p>
        <p>{{this.description}}</p>
        <p>{{this.code}}</p>
        <p>{{this.price}}</p>
        <p>{{this.status}}</p>
        <p>{{this.stock}}</p>
        <p>{{this.category}}</p>
        <p>{{this.id}}</p>
    </div>
    
    {{/each}}

</div>

<script src="/socket.io/socket.io.js"></script>
